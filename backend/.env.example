# Copy to .env (git-ignored) and adjust as needed.

# Comma-separated list of browser origins allowed to call the API directly.
# Only matters if the browser hits FastAPI without going through the Next.js
# proxy. "*" is fine for local development.
CORS_ORIGINS=*
